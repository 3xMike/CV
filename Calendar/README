реализован интерфейс базы данных календаря, хранящего записи в формате <дата,событие>, имеющий методы:
--Добавить событие
--Удалить события по логическому условию
--Найти события по логическому условию
--Распечатать все события
--Вывести последнее событие в определенный дне

Реализация
date: реализация структуры даты
node: реализация узла синтаксического дерева выражения
token: разбивает строку на токены
conditional_parser: по вектору токенов составляет синтаксическое дерево выражения
database: реализация хранилища и методов базы
main & tests: тесты + работа с интерфейсом
output: кастомный вывод в поток контейнеров map, vector, set, pair
test_runner: реализация интерфейса тестов (assert)
