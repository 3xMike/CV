Реализация поисковой системы: хранилище содержит набор страниц(документов),
а так же для каждого слова набор страниц, на которых оно использовалось.
Хранилище имеет следующий интерфейс:
-Добавление документа в базу
-Поиск по слову: на вход подается слово, на выход -- вектор документов (в которых это слово есть) и количество вхождений в каждый из них

Реализация
search_server: реализация интерфейса системы, реализация класса хранилища обратных индексов
parse: реализация функций разбора строк
test_runner: реализация интерфейса тестов (assert)
main: тесты, внешнее оформление
iterator_range: реализация функции Top

Основная идея проекта: максимально эффективная реализация путём выбора оптимальных контейнеров, обратных индексов,
а также грамотная передача ресурсов, в том числе move-семантика
Вторым этапом проекта было распараллеливание программы, но мне эта часть не досталась
